# IBOS 批量查询助手 — 代码全面审查报告

> 审查日期: 2026-08-08  
> 审查范围: 全部 Python 源码 (core/ + gui/) + 配置 + 启动脚本  
> 审查维度: 功能性 / 美观性 / 健壮性 / 工程性

---

## 一、总览

| 维度 | 评级 | 核心评价 |
|------|------|----------|
| 功能性 | B+ | 核心流程完整可用,但 `wait_mode` 智能等待是死代码,`order_phone` 导出有遗漏 |
| 美观性 | A- | macOS 风格一致,MacButton 自绘方案优秀,细节有瑕疵 |
| 健壮性 | B | 容错机制丰富(重试/自愈/校验),但异常吞没过多、线程安全有隐患 |
| 工程性 | C+ | 缺类型标注/测试/依赖清单,魔术数字多,部分代码组织松散 |

---

## 二、功能性 (Functionality)

### 🔴 P0 — `wait_mode` 智能等待完全未接入主流程

**文件**: `core/ibos_auto.py` L353-355

```python
def _wait_results(self):
    """固定等待 0.5 秒后开始保存..."""
    self.h.pause(0.5, 0.6)
```

`_wait_results` 是 `process_one` 调用的核心等待方法,但它**硬编码 0.5s**,完全无视 config 中的 `wait_mode`(`auto`/`screen_change`)、`result_wait_s`、`max_wait_s` 配置。

以下三个方法虽然实现了完整的智能等待逻辑,但**从未被调用**,属于死代码:
- `_wait_page_stable()` (L390) — AX 内容哈希变化检测
- `_wait_screen_change()` (L414) — 截图像素变化检测
- `_get_page_text_hash()` (L357) — AX 文本哈希

**影响**: 使用说明和 config 注释都声称 `wait_mode=auto` 时会"自动画面检测",但实际从未生效。用户按说明开启屏幕录制权限并标定结果区域后,不会获得任何加速效果。

**建议**: 在 `_wait_results` 中根据 `wait_mode` 分派:
```python
def _wait_results(self):
    mode = self.cfg.get("wait_mode", "auto")
    if mode == "screen_change":
        self._wait_screen_change()
    elif mode == "auto":
        area = self.cfg["points"].get("results_area") or [0,0,0,0]
        if area[2] > 0 and mac_perms.screen_recording_ok():
            self._wait_screen_change()
        else:
            self._wait_page_stable()  # AX 不可读则内部退回固定等待
    else:
        lo, hi = self.cfg.get("result_wait_s", [1.2, 1.8])
        self.h.pause(lo, hi)
```

### 🔴 P0 — `order_phone` 模式导出 Excel 缺少标识列与顺序保证

**文件**: `core/exporter.py` L337

```python
is_order = query_by == "order"
```

`order_phone` (按订购号码查询) 模式下 `is_order` 为 `False`,导出时:
1. 不会有「查询订单号」标识列
2. 不会按名单顺序输出
3. 查不到结果的号码不会保留空行

这与 `order` 模式的行为不一致,而 `order_phone` 在自动化流程 (`_input_point`) 中已完整支持。

**建议**: 将判断改为 `is_key_query = query_by in ("order", "order_phone")`,统一处理两种按键查询模式。

### 🟡 P1 — `export_excel` GUI 按钮未加载名单时 `query_order` 为空

**文件**: `gui/app.py` L622-624

```python
res = export_to_excel(files, out, self.logger,
                      query_by=self._qmode_name(),
                      query_order=list(self.names))
```

如果用户未点「加载并校验」直接点「导出 Excel」,`self.names` 为空列表。`order` 模式下 `query_order=[]` 会跳过按名单顺序输出的逻辑,退化为按文件名排序输出。

**建议**: 在 `export_excel` 开头检查并尝试从输入区自动加载,或在 `order` 模式下从文件名提取 query_order。

### 🟡 P1 — `_finalize` 后台导出线程无完成通知

**文件**: `core/ibos_auto.py` L175-197

导出在 daemon 线程中执行,如果导出失败,只写日志,GUI 上无任何反馈。用户可能看到"查询完成"通知后以为一切正常,但 Excel 实际生成失败。

**建议**: 提供回调机制或在 stats 中标记导出状态,GUI 轮询时检查并提示。

### 🟢 P2 — `_check_update` 是空壳

**文件**: `gui/app.py` L693-695

```python
def _check_update(self):
    messagebox.showinfo("检查更新", "当前已是最新版本(1.0)")
```

功能不存在却放在帮助菜单中,可能误导用户。建议移除或实现真实检查逻辑。

---

## 三、美观性 (Aesthetics)

### 🟡 P1 — 标定器步骤编号不一致

**文件**: `core/calibrate.py`

STEPS 列表定义了 ①②③④⑤⑥ 六步,但运行时 `on_release` 中结果区域的提示文字写的是 "⑤ 请再点击" (L100),而 STEPS 中结果区域是第 6 步 (⑥)。

```python
# L100: 应为 ⑥
self.txt.config(text="⑤ 请再点击「结果区域」右下角 (Enter 跳过)")
```

### 🟡 P1 — 标定器深色主题与主界面浅色主题不协调

**文件**: `core/calibrate.py` L39

标定器使用 `bg="#1e1e1e"` (深灰),而主界面是 `C_BG = "#EDEDF0"` (浅灰)。从浅色主界面切入全屏深色标定,视觉跳变较大。建议统一为浅色背景配深色文字,或加过渡动画。

### 🟢 P2 — 日志区深色背景与整体浅色风格割裂

`C_LOG_BG = "#1A1A1C"` 使日志区成为整个浅色界面中唯一的深色区块。这可以作为设计选择(终端风格),但建议提供浅色选项或与系统外观联动。

### 🟢 P2 — 迷你控制条位置硬编码

**文件**: `gui/app.py` L572

```python
self.mini.geometry("250x104+8+40")
```

固定在屏幕左上角 (8,40),在多显示器场景下可能出现在副屏。建议基于主屏尺寸计算位置。

### 🟢 P2 — 使用说明中残留 emoji 引用

`使用说明.txt` 中多处引用了「📋 选择名单文件」「📊 导出Excel」「▶ 开始查询」等 emoji,但代码中按钮文字已改为纯文字(如"导出 Excel"、"开始查询"、"从文件…")。文档与界面不同步。

---

## 四、健壮性 (Robustness)

### 🔴 P0 — `notify()` 存在 osascript 注入风险

**文件**: `core/mac_perms.py` L47-55

```python
def notify(title, message):
    subprocess.run(
        ["osascript", "-e",
         f'display notification "{message}" with title "{title}"'],
        ...)
```

`message` 和 `title` 直接拼入 AppleScript 字符串。如果消息内容含双引号(如错误信息 `保存失败:"xxx"`),会导致 osascript 语法错误甚至执行意外命令。

**建议**: 转义双引号或使用 `subprocess.run` 的参数列表传参:
```python
safe_msg = message.replace('"', '\\"').replace('\\', '\\\\')
safe_title = title.replace('"', '\\"').replace('\\', '\\\\')
```

### 🟡 P1 — `config.load()` 静默吞没所有异常

**文件**: `core/config.py` L81-90

```python
def load(path="config.json"):
    cfg = copy.deepcopy(DEFAULT)
    if os.path.exists(path):
        try:
            ...
        except Exception:
            pass   # ← JSON 语法错误也静默忽略
    return cfg
```

如果用户手改 config.json 导致 JSON 语法错误,程序会用默认配置启动,用户完全不知情。所有标定坐标丢失却无任何提示。

**建议**: 至少 `print` 或 `logging.warning` 提示解析失败,或向上抛出让调用方处理。

### 🟡 P1 — `_wait_panel_open` 在检测信号不可用时盲目返回 True

**文件**: `core/ibos_auto.py` L619-621

```python
else:  # None = 检测信号完全不可用,按已打开处理,避免卡死
    time.sleep(0.2)
    return True
```

当 AX 和 CGWindow 两个信号都不可用时(如权限被撤销),只等 0.2s 就认为面板已打开,后续按键可能落到页面上而非保存面板。

**建议**: 信号不可用时应退回固定等待 `save_dialog_delay_s` 秒,而非 0.2s。

### 🟡 P1 — `_archive_previous_batch` 使用 `shutil.move` 可能覆盖同名文件

**文件**: `core/ibos_auto.py` L673

```python
shutil.move(fp, os.path.join(archive_dir, os.path.basename(fp)))
```

如果归档目录已有同名文件(极短时间内跑两批),`shutil.move` 会覆盖。建议先检查目标是否存在,存在则追加时间戳。

### 🟡 P1 — `Calibrator` 创建第二个 `Tk()` 实例

**文件**: `core/calibrate.py` L35

```python
def start(self):
    self.root = tk.Tk()  # ← 第二个 Tk 实例
```

主应用 `app.py` 已有一个 `tk.Tk()`。同一进程内创建多个 `Tk()` 实例可能导致事件循环冲突、变量不共享等问题。

**建议**: 改用 `tk.Toplevel(self.root)` 并隐藏主窗口,或销毁主窗口后在同进程重建。

### 🟡 P1 — `_find_stray_save` 类级可变缓存

**文件**: `core/ibos_auto.py` L633

```python
cache = getattr(IbosAutomation, "_stray_cache", {})
```

使用类属性作为缓存,在测试或多实例场景下会共享状态。虽然当前单实例使用没问题,但不够健壮。

**建议**: 改为实例属性 `self._stray_cache = {}`,在 `__init__` 中初始化。

### 🟢 P2 — 大量 `except Exception: pass` 吞没异常

全项目至少 15 处 `except Exception: pass` 或等效写法,部分位置(如 `_write_progress` L752、`_save_screenshot` L709)确实不需要中断主流程,但有些位置(如 `config.load`、`_finalize` 的导出异常)静默吞没会导致问题难以排查。

**建议**: 至少 `self.log.warn(f"...")` 记录异常信息,而非完全静默。

### 🟢 P2 — `_wait_file` 轮询使用 `os.listdir` 性能差

**文件**: `core/ibos_auto.py` L683-696

导出目录可能含 1000+ 文件,每次 `os.listdir` 遍历全目录。轮询间隔 0.1s,保存等待 30s 内可能调用 300 次。

**建议**: 使用 `os.path.exists(os.path.join(out, safe + ext))` 逐扩展名检查,避免遍历整个目录。

---

## 五、工程性 (Engineering)

### 🔴 P0 — 缺少 `requirements.txt`

整个项目没有依赖清单。依赖散落在启动脚本的 `pip install` 行和代码的 `import` 中:
- `pyautogui`, `pyperclip`, `Pillow`
- `pyobjc-framework-ApplicationServices`, `pyobjc-framework-Quartz`, `pyobjc-framework-AppKit`
- `beautifulsoup4`, `lxml`, `openpyxl`

**建议**: 创建 `requirements.txt`:
```
pyautogui>=0.9.54
pyperclip>=1.8.2
Pillow>=10.0
pyobjc-framework-ApplicationServices>=10.0
pyobjc-framework-Quartz>=10.0
pyobjc-framework-Cocoa>=10.0
beautifulsoup4>=4.12
lxml>=5.0
openpyxl>=3.1
```

### 🔴 P0 — 缺少测试

项目有 1000+ 行业务逻辑(尤其是 `exporter.py` 的 MHTML 解析),但没有任何测试文件。解析逻辑高度依赖真实 MHTML 结构,任何改动都可能引入回归。

**建议**: 至少为 `exporter.py` 的核心解析函数添加单元测试,使用真实 MHTML 样本(脱敏后)作为测试夹具。

### 🟡 P1 — 无类型标注

全部函数无 `type hints`,对于含复杂 dict 结构的方法(如 `export_to_excel` 的 `run_stats` 参数),调用方需要读源码才能知道 dict 的 key。

**建议**: 逐步添加类型标注,优先标注公共接口和复杂参数。

### 🟡 P1 — 魔术数字过多

散布在各处的硬编码数值:
- `8 * 1024` — 文件最小大小 (L467)
- `128 * 1024` — 读取块大小 (L479)
- `0.12` — 轮询间隔 (L405)
- `0.1` — 文件轮询间隔 (L689)
- `20` — AX 子元素最大取数 (L380)
- `200, 1200, 150, 700` — 面板尺寸区间 (L252)
- `200, 100` — 窗口最小尺寸 (L88)
- `48` — 订单号输入框偏移量 (L282)

**建议**: 提取为模块级常量,带注释说明取值原因。

### 🟡 P1 — 重复 import

**文件**: `core/ibos_auto.py`

```python
# L7: 顶层
import datetime
# L53: 函数内重复
import datetime
```

```python
# L658: 函数内
import datetime, glob as _glob
```

`datetime` 在顶层已导入,函数内重复导入无意义。`glob` 应在顶层导入。

### 🟡 P1 — `_add_report_sheet` 硬编码行号

**文件**: `core/exporter.py` L455-510

```python
sec(5, "一、总览")       # 第 5 行
# overview 5 条 → 第 6-10 行
sec(13, "二、各姓名明细") # 第 13 行(硬编码,依赖 overview 恰好 5 条)
ws.cell(row=14, ...)      # 表头第 14 行
r = 15                    # 数据从第 15 行开始
```

如果 overview 条目数变化,所有后续行号需要手动调整。

**建议**: 用变量跟踪当前行号,动态计算:
```python
r = 5
sec(r, "一、总览"); r += 1
for k, v in overview:
    ws.cell(row=r, ...); r += 1
r += 1  # 空行
sec(r, "二、各姓名明细"); r += 1
# ...
```

### 🟡 P1 — `app.py` `_log` 方法脆弱

**文件**: `gui/app.py` L244-245

```python
def _log(self, level, msg):
    getattr(self.logger, level.lower() if level != "SYS" else "info")(msg)
```

如果 `level` 是 `"SYS"`,调用 `self.logger.info`。但如果 `level` 是其他未预料的值(如 `"DEBUG"`),`getattr` 会抛 `AttributeError`。

**建议**: 使用映射表或 `getattr(self.logger, level.lower(), self.logger.info)`。

### 🟢 P2 — `_LABEL_RE` 只匹配全角冒号

**文件**: `core/exporter.py` L25-26

```python
_LABEL_RE = re.compile(
    "(" + "|".join(...) + r")\s*：")  # 只匹配全角 ：
```

如果 IBOS 页面改用半角冒号 `:`,所有字段解析将失效。

**建议**: 同时匹配全角和半角: `r")\s*[：:]")`

### 🟢 P2 — 启动脚本路径硬编码 ✅ 已于 08-08 修复

**文件**: `启动查询工具.command` L4, L17-18

修复前(绝对路径绑定到特定机器,换机器或升级 Python 版本后会失效):

```bash
PY="$HOME/.workbuddy/binaries/python/envs/ibos-scraper/bin/python"
export TCL_LIBRARY="$PY_PREFIX/lib/tcl9.0"
```

绝对路径绑定到特定机器,换机器或升级 Python 版本后会失效。

**建议**: 使用相对路径或环境变量探测:
```bash
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PY="${SCRIPT_DIR}/.venv/bin/python"  # 或用 which python3
```

### 🟢 P2 — `_validate_input` 内重复 import re

**文件**: `gui/app.py` L339, L344

```python
def _validate_input(self, mode):
    ...
    if mode == "order":
        import re  # ← L339
    elif mode == "order_phone":
        import re  # ← L344
```

`re` 已在文件顶部 L9 导入,函数内重复导入冗余。

### 🟢 P2 — `write_text_report` 引用 `per_key_orders` 但 stats 可能不含此 key

**文件**: `core/exporter.py` L534

```python
cnt = stats.get("per_key_orders", {}).get(item["name"], 0)
```

`per_key_orders` 只在 `export_to_excel` 中通过 `run_stats.update(result)` 写入。如果 `write_text_report` 被独立调用(如 `_finalize` 中导出失败但仍执行报告生成),`per_key_orders` 不存在,虽然 `.get({})` 兜底了但逻辑不完整。

---

## 六、改进优先级排序

| 优先级 | 问题 | 影响 |
|--------|------|------|
| **P0** | `wait_mode` 智能等待死代码 | 用户按说明操作但功能不生效 |
| **P0** | `order_phone` 导出缺标识列 | 导出结果不完整 |
| **P0** | `notify()` osascript 注入 | 安全风险 |
| **P0** | 缺 `requirements.txt` | 环境不可复现 |
| **P0** | 缺测试 | 解析逻辑回归无保障 |
| **P1** | config.load 静默吞异常 | 配置错误无提示 |
| **P1** | `_wait_panel_open` 信号不可用时 0.2s 返回 | 按键可能落错窗口 |
| **P1** | Calibrator 第二个 Tk() 实例 | 潜在事件循环冲突 |
| **P1** | 标定器步骤编号不一致 | 用户混淆 |
| **P1** | `_add_report_sheet` 硬编码行号 | 维护困难 |
| **P1** | 魔术数字过多 | 可读性差 |
| **P1** | `_finalize` 后台导出无反馈 | 导出失败用户不知 |
| **P2** | 大量 except Exception: pass | 问题难排查 |
| **P2** | `_wait_file` 轮询性能 | 大目录下慢 |
| **P2** | 启动脚本硬编码路径 | 不可移植 |
| **P2** | 无类型标注 | 可维护性差 |

---

## 七、总体评价

项目在**特定场景下(单人单机使用)是可用的**,容错机制(重试/自愈/校验/归档)考虑得相当周到,能看出经过多轮实机调试迭代。MacButton 自绘方案是一个亮点,解决了 macOS 下 tk.Button 配色的根本问题。

主要短板在**工程规范**层面:缺测试、缺依赖清单、缺类型标注、魔术数字多。这些不影响当前使用,但会显著增加后续维护和交接成本。建议优先修复 P0 项(尤其是 `wait_mode` 死代码和 `order_phone` 导出遗漏),再逐步补齐工程基础设施。
